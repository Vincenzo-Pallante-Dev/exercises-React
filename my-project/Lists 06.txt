Lists 06

Modify the `TodoList` by adding a "remove" `button` to each `li` tag. When clicked, the event handler should remove corresponding item from the `items` array.
Modifica la TodoList aggiungendo un pulsante "rimuovi" a ciascun tag li. Quando si fa clic, il gestore eventi dovrebbe rimuovere l'elemento corrispondente dall'array di elementi.
