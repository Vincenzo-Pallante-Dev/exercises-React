Add a new Route to the `/counter` path that renders the `Counter` component from useState 01.
Aggiungi una nuova Route al percorso "/counter" 
che esegue il rendering del componente "Counter" da useState 01.