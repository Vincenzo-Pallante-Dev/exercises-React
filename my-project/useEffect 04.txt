Create a GithubUserList component that maintains an array of usernames, showing a `GithubUser` component for each username entered. The usernames should be added to the array using an input field and a button.
Crea un componente GithubUserList che mantiene un array di nomi utente,
 mostrando un componente `GithubUser` per ogni nome utente inserito.
  I nomi utente devono essere aggiunti all'array utilizzando un campo di input e un pulsante.