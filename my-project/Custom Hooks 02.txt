Create a custom hook that keeps track of the state of a controlled form with the `username` and `password` inputs, and returns the current value of the inputs as well as an event handler to update either input.
Crea un hook personalizzato che tiene traccia dello stato di un modulo controllato
 con gli input "nome utente" e "password"
  e restituisce il valore corrente degli input
   nonché un gestore di eventi per aggiornare entrambi gli input.