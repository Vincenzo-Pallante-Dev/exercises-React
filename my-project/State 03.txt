State 03

Modify the `Counter` component so that the initial value of the counter, the increment interval and the increment amount are passed as props to the component.
Modifica il componente "Contatore" in modo che il valore iniziale del contatore, 
l'intervallo di incremento e l'importo dell'incremento vengano passati come oggetti di scena al componente.